import type { Job, Option } from '@/lib/types';
import { saveJob } from '@/app/actions';
import { ActionForm } from './action-form';
export function JobForm({ cities, categories, job }: {
    cities: Option[];
    categories: Option[];
    job?: Job;
}) { return <ActionForm action={saveJob} label="Сохранить и посмотреть карточку">{job && <input type="hidden" name="id" value={job.id}/>}<input type="hidden" name="photo_url" value={job?.photo_url || ''}/><div className="form-grid"><label className="span">Название<input name="title" required minLength={2} maxLength={200} defaultValue={job?.title} placeholder="Грузчик на склад"/><small>Коротко укажите, кого ищете.</small></label><label>Город<select name="city_id" defaultValue={job?.city_id} required>{cities.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select><small>Город работы, не регистрации компании.</small></label><label>Категория<select name="category" defaultValue={job?.category || ''}><option value="">Не указана</option>{categories.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select><small>Помогает найти объявление.</small></label><label className="span">Описание<textarea name="description" minLength={10} maxLength={20000} required defaultValue={job?.description} placeholder="Разгрузка товара, работа 8 часов, оплата после смены"/><small>Задачи, требования, условия и порядок оплаты.</small></label><label>Оплата от, ₽<input type="number" name="salary_min" min={0} max={100000000} step="0.01" defaultValue={job?.salary_min ?? ''} placeholder="4500"/><small>Если сумма неизвестна, оставьте пустым.</small></label><label>Оплата до, ₽<input type="number" name="salary_max" min={0} max={100000000} step="0.01" defaultValue={job?.salary_max ?? ''} placeholder="5000"/><small>Для фиксированной оплаты можно не заполнять.</small></label><label>Единица оплаты<select name="salary_type" defaultValue={job?.salary_type || ''}><option value="">Не указана</option><option value="shift">За смену</option><option value="hour">За час</option><option value="task">За задачу</option><option value="month">За месяц</option></select><small>Суммы за смену и месяц не смешиваются в фильтрах.</small></label><label>Когда платите<select name="payment_type" defaultValue={job?.payment_type || ''}><option value="">Не указано</option><option value="immediate">Сразу после работы</option><option value="daily">Ежедневно</option><option value="weekly">Еженедельно</option><option value="monthly">Ежемесячно</option><option value="other">Другой порядок</option></select><small>Уточните детали в описании.</small></label><label>Дата начала<input type="date" name="date_start" defaultValue={job?.date_start || ''}/><small>Необязательно. Только известная дата.</small></label><label>Дата окончания<input type="date" name="date_end" defaultValue={job?.date_end || ''}/><small>После неё объявление перестанет показываться.</small></label><label>Время начала<input type="time" name="time_start" defaultValue={job?.time_start?.slice(0, 5) || ''}/><small>Местное время города.</small></label><label>Время окончания<input type="time" name="time_end" defaultValue={job?.time_end?.slice(0, 5) || ''}/><small>Ночная смена может заканчиваться утром.</small></label><label className="span">Адрес<input name="address_raw" maxLength={500} defaultValue={job?.address_raw || ''} placeholder="ул. Большевистская, 45"/><small>Ссылки на 2ГИС и Яндекс Карты создаются автоматически. Не знаете адрес, оставьте поле пустым.</small></label><label>Телефон<input name="contact_phone" type="tel" defaultValue={job?.contact_phone || ''} placeholder="+7 900 000-00-00"/><small>Будет опубликован для связи.</small></label><label>Telegram<input name="contact_telegram" defaultValue={job?.contact_telegram || ''} placeholder="@username"/><small>Личный username, без ссылки на сообщение.</small></label><label>Email<input type="email" name="contact_email" defaultValue={job?.contact_email || ''} placeholder="work@example.org"/><small>Необязательно, будет виден посетителям.</small></label><label>Тип занятости<input name="employment_type" maxLength={60} defaultValue={job?.employment_type || ''} placeholder="Временная работа"/><small>Разовая смена, временная или постоянная работа.</small></label><label className="span">Фото<input type="file" name="photo" accept="image/jpeg,image/png"/><small>JPEG или PNG, не больше 800 КБ. Не загружайте документы и персональные данные.</small></label></div></ActionForm>; }
