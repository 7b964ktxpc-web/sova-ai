import { ActionForm } from './action-form';
import { saveEmployer } from '@/app/actions';
import type { Option } from '@/lib/types';
export function EmployerForm({ cities, employer: e }: {
    cities: Option[];
    employer?: Record<string, string> | null;
}) { return <ActionForm action={saveEmployer} label="Сохранить профиль"><input type="hidden" name="logo_url" value={e?.logo_url || ''}/><div className="form-grid"><label>Компания<input name="name" required minLength={2} maxLength={160} defaultValue={e?.name} placeholder="Название компании"/><small>Название, которое увидят соискатели.</small></label><label>Адрес страницы<input name="slug" required pattern="[a-z0-9][a-z0-9-]{2,70}" defaultValue={e?.slug} placeholder="company-nsk"/><small>Латиница, цифры и дефис, от 3 символов.</small></label><label className="span">О компании<textarea name="description" maxLength={10000} defaultValue={e?.description} placeholder="Чем занимаетесь и какие смены предлагаете"/><small>Не добавляйте чужие персональные данные.</small></label><label>Город<select name="city_id" defaultValue={e?.city_id} required>{cities.map(c => <option value={c.id} key={c.id}>{c.name}</option>)}</select><small>Основной город работы.</small></label><label>Контактное лицо<input name="contact_person" maxLength={120} defaultValue={e?.contact_person} placeholder="Имя менеджера"/><small>Будет видно в публичном профиле.</small></label><label>Телефон<input name="contact_phone" type="tel" defaultValue={e?.contact_phone} placeholder="+7 900 000-00-00"/><small>Публичный рабочий номер.</small></label><label>Telegram<input name="contact_telegram" defaultValue={e?.contact_telegram} placeholder="@username"/><small>Публичный контакт для связи.</small></label><label>Сайт<input name="website" type="url" defaultValue={e?.website} placeholder="https://example.org"/><small>Только https://.</small></label><label>Логотип<input type="file" name="logo" accept="image/jpeg,image/png"/><small>JPEG или PNG до 800 КБ.</small></label></div></ActionForm>; }
